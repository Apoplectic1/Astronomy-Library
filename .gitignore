# Build output
[Bb]in/
[Oo]bj/
*.pdb
*.dll.config

# Vendored PixInsight Class Library — source + build state, never published from here.
# Canonical versioned home: https://github.com/Apoplectic1/PCL (its own repo, cloned nested
# at Library\PCL\ — this ignore keeps the two repos from interacting). See RELEASING.md.
/PCL/

# Visual Studio IDE state
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.VC.db
*.VC.VC.opendb

# ReSharper / Rider
_ReSharper*/
*.DotSettings.user

# NuGet packages (regenerate with `nuget restore`)
packages/

# Test / coverage output
TestResults/
[Cc]overage*/

# BenchmarkDotNet per-run output (logs, reports, artifacts)
BenchmarkDotNet.Artifacts/

# OS noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Emacs temp / backup / autosave / lock
*~
\#*\#
.\#*
TAGS
*.elc

# Claude Code: machine-local permission accretion stays out; commands/skills/settings.json are tracked
.claude/settings.local.json
